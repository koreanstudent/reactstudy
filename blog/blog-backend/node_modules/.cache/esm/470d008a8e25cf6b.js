let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_8e2‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_8e2‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_8e2‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_8e2‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_8e2‍.w("./api",[["default",["api"],function(v){api=v}]]);_8e2‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








// import createFakeData from './createFakeData';

// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } =process.env;

// 서버와 데이터베이스를 연결
mongoose.connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false})
          .then (() => {
            console.log('connected to MongoDB');
            // createFakeData();
          })
          .catch(e => {
            _8e2‍.g.console.error(e);
          })

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());


// PORT가 지정되어 있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
  _8e2‍.g.console.log('listening to port 4000',port);
});